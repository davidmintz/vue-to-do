# where Symfony is listening *today*
VITE_PROXY_TARGET=https://127.0.0.1:8000

# make frontend call relative URLs so Vite can proxy them
# (leave empty → we’ll build URLs like /api/tasks)
VITE_API_BASE=/api
